[package]
name = "repository_common"
version = "0.1.0"
edition = "2021"

[dependencies]
network = { path = "../network" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
thiserror = "1.0"
async-trait = "0.1"
tokio = { version = "1", features = ["time"] }
urlencoding = "2.1"
